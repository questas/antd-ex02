import Vue from 'vue'
import { Button, Form, Input, message, Select, Table } from 'ant-design-vue'

Vue.use(Button)
Vue.use(Form)
Vue.use(Input)
Vue.use(Select)
Vue.use(Table)

Vue.prototype.$message = message
